name: root_navigator_example
description: root_navigator example.
version: 0.0.1
homepage: https://github.com/Gerrel/root_navigator

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  root_navigator:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true