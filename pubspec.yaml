name: root_navigator
description: A flutter package that creates a sub-navigator with routes (independent view stack)
version: 1.0.5
homepage: https://github.com/Gerrel/root_navigator

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
